# Planing Ledger

## 1. Purpose

This ledger tracks planning and implementation history for the SynCinema ClaudeQB run so that future Step 2 replanning and Step 4 implementation sessions have durable memory of what was planned, what was implemented, what validation passed or failed, and what should be read before making new decisions. This is the first ledger created for this repository — no prior planning or implementation run existed before this session (Step 1/Step 1.5/Step 2, all executed in this same overall ClaudeQB run).

## 2. Planning Runs

| Run | Step | Date | Scope | Output |
|---|---|---|---|---|
| Run 1 | Step 1 | 2026-07-21 | Initial main plan for SynCinema (existing project). | `Planner-docs/Main-Planing.md` |
| Run 1 | Step 1.5 | 2026-07-21 | Autopsy of existing repository state, ontology extraction. | `Planner-docs/Autopsy.md`, `Planner-docs/Project-Ontology.md` |
| Run 1 | Step 2 | 2026-07-21 | Sub-phase decomposition of all 7 main phases into 21 implementation-ready sub-plans. | `Planner-docs/Sub-Planing-Index.md`, `Planner-docs/Faz-1-Plans/` through `Planner-docs/Faz-7-Plans/`, this ledger (initialized) |

No prior implementation run exists. No prior ledger existed before this session; this is a fresh Ledger v2 initialization, not an upgrade from a legacy v1 file.

## 3. Plan Snapshot Registry

| Artifact | Status | Source evidence | Notes |
|---|---|---|---|
| Planner-docs/Main-Planing.md | active | Step 1 intake + repository read | 7-phase roadmap; Phase 7 explicitly deferred by user's own stated position. |
| Planner-docs/Autopsy.md | active | Step 1.5 read-only repository review, full 90-commit history | Confirms Main Plan's feature/gap claims; source of AUTOPSY-P0/P1/P2/P3 findings referenced throughout Step 2 sub-plans. |
| Planner-docs/Project-Ontology.md | active | Step 1.5, derived from repository evidence | Domain vocabulary, invariants (compressor bypass, AudioContext lifecycle, drift threshold) that Step 2 and future Step 4 work must not silently violate. |
| Planner-docs/Sub-Planing-Index.md | active | Step 2, this run | Maps all 7 phases to their 21 sub-plans. |
| Planner-docs/Faz-1-Plans/ through Faz-7-Plans/ | active | Step 2, this run | 21 sub-plan files; see Section 4 below for individual status. |

## 4. Sub-Plan Status Matrix

| Sub-plan Path | Status | Snapshot ID | Run ID | Validation Evidence | Blocker | Next Action | Superseded By | Updated At |
|---|---|---|---|---|---|---|---|---|
| Planner-docs/Faz-1-Plans/Faz1.1-typecheck-in-build.md | verified | Run 1 | Run 2 | `npx tsc --noEmit` baseline: 0 errors (F1.1-01, no triage needed). `npm run build` fails non-zero (exit 2) with deliberate type error, exits 0 clean after revert (F1.1-05, verified via `git diff` showing only `package.json` changed). | none | Faz1.2 (ESLint) may begin. | — | 2026-07-22 |
| Planner-docs/Faz-1-Plans/Faz1.2-eslint-configuration.md | verified | Run 1 | Run 2 | Baseline with `eslint-plugin-react-hooks@7.1.1`'s full `recommended` config: 51 problems (24 errors, 27 warnings) — rejected as scope overflow (bundles new React Compiler-era rules, e.g. `set-state-in-effect`/`immutability`, not requested by the sub-plan). Narrowed config to `rules-of-hooks`+`exhaustive-deps` only, matching sub-plan Section 4. Final: `npm run lint` exit 0 (0 errors, 27 warnings, explicitly triaged as accepted below). `npm run typecheck` still exit 0 (no regression). | none | Faz1.3 (CI) may begin; both Faz1.1 and Faz1.2 acceptance criteria are met. | — | 2026-07-22 |
| Planner-docs/Faz-1-Plans/Faz1.3-minimal-ci-workflow.md | verified | Run 1 | Run 2 (local) / Run 6 (live, post-push) | `.github/workflows/ci.yml` authored (checkout, setup-node@22, `npm ci`, typecheck→lint→test→build). Local pre-check all exit 0. **F1.3-02 live confirmation (2026-07-22, post-push of commit `5c0768f`):** user pushed to `origin/main`, screenshotted GitHub Actions — "CI #1: Commit 5c0768f pushed by RuslanAeff", green check, 35s, all steps passed. Real Actions run confirmed working. F1.3-03 (throwaway-branch negative-path proof) and F1.3-04 (branch-protection rule) still not done — optional, lower-value now that a real passing run exists. | none | Optional: user may enable branch protection ("require status checks to pass") on `main` in GitHub Settings → Branches, referencing this now-proven CI workflow. | — | 2026-07-22 |
| Planner-docs/Faz-1-Plans/Faz1.4-dead-weight-removal.md | verified | Run 1 | Run 2 | F1.4-01: re-confirmed zero `wavesurfer` references (grep, exit 1/no matches). F1.4-02: `npm uninstall wavesurfer.js` — removed from package.json/package-lock.json. F1.4-03: traced `onAudioUrlLoad` — found it ALSO feeds `Sidebar`→`AudioSection.tsx`'s real, reachable "add audio from URL" UI (not just the dead `UrlLoaderModal` tab); this was the exact risk Section 11 flagged. F1.4-04: removed only `UrlLoaderModal.tsx`'s dead `activeTab`/`audioUrl`/`audioValidation`/`handleLoadAudio`/`onAudioUrlLoad` prop and the now-unused `Music` import, plus the dead `onAudioUrlLoad={handleAudioFromUrl}` wiring at `App.tsx:614`; the live `handleAudioFromUrl` function and its Sidebar wiring (`App.tsx:573`) were left untouched. F1.4-05: `npm run typecheck`/`lint`/`build` all exit 0 (lint warnings dropped 27→22 as the deleted dead code's own unused-var warnings disappeared). Manual browser golden-path check could not be performed (no chromium-cli/Playwright/local browser in this environment, and user chose not to install Playwright mid-slice); substituted with a full line-by-line diff review confirming the video-URL path (`validateUrl`, `convertGoogleDriveUrl`, `extractFilename`, `handleVideoUrlChange`, `handleLoadVideo`, `handleClose`, all video-form JSX) is byte-for-byte unchanged. | none | Phase 1 complete; Phase 2 (Security Hardening) may begin. | — | 2026-07-22 |
| Planner-docs/Faz-2-Plans/Faz2.1-proxy-referer-exact-match.md | verified | Run 1 | Run 3 | F2.1-01: reproduced bypass — `startsWith` check on crafted referer `https://syncinema.vercel.app.attacker.example/evil-page` returned `true` (vulnerable) before the fix. F2.1-02: replaced with `new URL(referer).origin` exact-match against `ALLOWED_ORIGINS`, wrapped in try/catch (deny on parse failure). F2.1-03/04: 5-case scripted check — crafted bypass now rejected, legitimate prod/dev origins still pass, unrelated malicious referer rejected, malformed referer safely denied (no throw). `npm run typecheck`/`lint`/`build` all still pass (0 errors, 22 warnings, unchanged). | none | Faz2.2 may proceed independently (no dependency). AUTOPSY-P1-03 closed. | — | 2026-07-22 |
| Planner-docs/Faz-2-Plans/Faz2.2-server-side-admin-lockout.md | verified | Run 1 | Run 3 (local) / Run 6 (live) | F2.2-01/02 (Run 3, local): designed `supabase/migrations/0001_admin_lockout_serverside.sql`. **F2.2-03/04 applied live 2026-07-22 (Run 6), guided step-by-step with the user directly in the Supabase dashboard.** Before wiring, the user pasted the actual live bodies of `verify_admin_password`/`admin_delete_preset` — **major plan-vs-reality discovery**: both already had a separate, pre-existing GLOBAL (not per-IP) brute-force gate (`login_attempts` table, 5-failures/5-min global counter, 1s `pg_sleep()` throttle, hashed `crypt()` password compare) that no planning document (Main-Planing/Autopsy/AUTOPSY-P1-01/Faz2.2 itself) knew about — Step 2 planning had no live access when it assumed no server-side protection existed at all. Confirmed via `select proname, prosecdef from pg_proc` that both functions are `SECURITY DEFINER` (prosecdef=true) before touching them. **User-confirmed decision: layer Faz2.2's per-IP lockout on top of the existing global gate as an additional, independent check (defense in depth) rather than replacing or skipping it.** Wrote exact `CREATE OR REPLACE FUNCTION` statements preserving 100% of the existing global-gate/hash/throttle logic unchanged, adding only the new `admin_lockout_is_locked`/`record_failure`/`reset` calls at the entry point and at the existing success/failure branches. All 3 SQL blocks (helper functions+table with RLS enabled, `verify_admin_password`, `admin_delete_preset`) ran with "Success" in the dashboard. Captured both the pre-change bodies (`supabase/migrations/0000_baseline_asis.sql`, reconstructed from what the user pasted) and the as-applied bodies (`supabase/migrations/0002_admin_lockout_integration_applied.sql`) as real migration files in the repo. `supabase/README.md` updated to reflect live status. **Live behavioral test completed and confirmed 2026-07-22:** 3 wrong passwords triggered the client-side UX lock as before; after a page reload (client counter reset) and an immediate retry, Safari's Web Inspector console showed the RPC itself rejecting with `{code: "P0001", message: "Rate limited: too many failed attempts from this location. Try again in 30 seconds."}` — direct proof the new per-IP gate (not just the pre-existing global one) fired server-side. After the 30s window elapsed, the user reset their forgotten admin password via `UPDATE admin_config SET value = crypt(new_password, gen_salt('bf')) WHERE key = 'admin_password_hash'` (bcrypt via pgcrypto, same scheme `crypt()` already validates against) and logged in successfully, confirming the full flow end-to-end: lockout expiry → `admin_lockout_reset` → correct-password path all work together. `typecheck`/`lint`/`build` unaffected (no frontend files touched — `AdminPanel.tsx`'s existing generic `catch` already handles any RPC exception, verified by reading it before making this decision). | — | 2026-07-22 |
| Planner-docs/Faz-2-Plans/Faz2.3-supabase-rpc-versioning.md | verified_local_only | Run 1 | Run 3 (local) / Run 6 (partial baseline) | F2.3-01 (complete): narrowed `.gitignore`'s `*.sql` blanket exclusion. F2.3-02/03 (Run 6, partial): `0000_baseline_asis.sql` now exists, but only covers `verify_admin_password`/`admin_delete_preset` (captured as a side effect of Faz2.2's live session, from what the user pasted from the dashboard) — **not** a full `pg_dump --schema-only`, and `safe_insert_preset`/`safe_increment_vote`/`sync_presets` RLS policies are still uncaptured. F2.3-04: `supabase/README.md` updated to reflect Faz2.2's live status. | Still missing: full baseline export for the other 2 RPCs + RLS policies. | User (has live dashboard/CLI access) should still run a full `pg_dump --schema-only` or copy the remaining 2 function definitions + `sync_presets` RLS policies from the dashboard to complete `0000_baseline_asis.sql`. | — | 2026-07-22 |
| Planner-docs/Faz-2-Plans/Faz2.4-microphone-permission-gesture-gate.md | verified | Run 1 | Run 3 | F2.4-01: traced `refreshDevices`/`permissionsGranted` downstream (App.tsx → Sidebar.tsx → SidebarHeader.tsx) and found an already-existing, already-wired "Grant Permission" button (`SidebarHeader.tsx:70-78`, `onClick={onRefreshDevices}`, conditionally shown when `!permissionsGranted`) — it was simply redundant because the mount-time effect fired the prompt first. F2.4-02: removed the unconditional `refreshDevices()` call from `useAudioTracks.ts`'s mount `useEffect`, kept the `devicechange` listener registration unchanged. F2.4-03: no new wiring needed — the existing button already calls the right handler. Confirmed via `grep` that `getUserMedia` has exactly one call site (`refreshDevices`), now only reachable via the button. F2.4-04: no browser automation available in this environment (same gap as Faz1.4); substituted a full code-trace of the logic path (mount→no prompt, button click→prompt→`permissionsGranted=true`→device list populates→button hides) instead of a live browser test. `typecheck`/`lint`/`build` all pass, unchanged. | none | Faz2.5 may proceed independently. AUTOPSY-P2-01 closed (pending live browser confirmation by the user). | — | 2026-07-22 |
| Planner-docs/Faz-2-Plans/Faz2.5-sync-import-shape-validation.md | verified | Run 1 | Run 3 | F2.5-01: new `src/utils/syncImportValidation.ts` — `sanitizeImportedTrackPref`, `sanitizeImportedAppSettings`, `hadInvalidKnownField`, whitelisting exactly the 6 fields `saveTrackPref` persists, with bounds sourced from real UI sliders (`playbackRate` 0.5-2.0, `gainBoost` 1-3, `masterVolume` 0-1 — all confirmed via `grep`/`Read` of the actual `min`/`max` slider attributes, not invented) plus a defensive ±24 EQ-band ceiling (no UI bound exists there; `AudioGraphManager.tsx` applies eq values unclamped to `BiquadFilterNode.gain`). `offset` deliberately left unbounded (the number input has no min/max — app supports arbitrary ± offsets per README). F2.5-02: applied in `importProject` — critically, sanitized *before* the `localStorage.setItem('synCinema_trackPrefs', ...)` write too, not just before the live `setAudioTracks` update, since `addAudioTracks`/`addAudioFromUrl` read raw `getStoredPrefs()` later and a delayed-activation bypass was possible otherwise (a malicious field could corrupt a track added *after* import, even if the currently-loaded tracks were clean). F2.5-03: toast message now reports "Some settings were invalid and were skipped" when `hadInvalidKnownField` fires on any track. F2.5-04: transpiled the pure sanitizer via `esbuild` and ran 4 direct test scenarios (malicious payload with extra/wrong-typed/out-of-range fields, well-formed regression case, appSettings validation, non-object/null/array inputs) — all passed exactly as expected; `id`/`objectUrl`/`file` never leak through, valid fields pass unchanged. `typecheck`/`lint`/`build` all pass (0 errors, 22 warnings, unchanged). | none | Phase 2 fully closed (pending Faz2.2/Faz2.3 live-execution follow-ups, tracked separately). Phase 3 may begin. `sanitizeImportedTrackPref`/`sanitizeImportedAppSettings` flagged as ready-made Faz4.2/4.3 unit-test targets. | — | 2026-07-22 |
| Planner-docs/Faz-3-Plans/Faz3.1-readme-correction.md | verified | Run 1 | Run 4 | F3.1-01 full cross-check (`find src api` vs. README tree) surfaced more drift than the two known items: `VuMeter.tsx` (deleted, known), dev port 5173→3000 (known), plus newly found — `SubtitleOverlay.tsx` (real file, undocumented), the `src/types/` subdirectory (`dom-extensions.d.ts`, `youtube.d.ts`, undocumented), `src/utils/syncImportValidation.ts` (added by this session's own Faz2.5, now real), and "Root-level extras" claiming 4 root `*_rpc.sql` files that do not exist anywhere on disk (superseded by this session's real `supabase/migrations/`). `detached.tsx` was already correctly documented (false alarm, not drift). All corrected in this same pass per F3.1's own instruction not to defer additional drift. F3.1-04: started `npm run dev` in background, confirmed Vite's actual printed URL is `http://localhost:3000/`, matching the corrected README exactly; dev server stopped and port freed after. No source-code gates affected (docs-only change). | none | Faz3.2/Faz3.3 may proceed independently. | — | 2026-07-22 |
| Planner-docs/Faz-3-Plans/Faz3.2-whitepaper-correction.md | verified | Run 1 | Run 4 | F3.2-01 audit confirmed the 3 known EN claims (line 14 "open-source", line 55 "React 18", line 143 "builds upon...open-source") plus TR equivalents, all cross-referenced against `LICENSE` (Proprietary, source-available for education/personal/research only) and `package.json` (`"react": "^19.2.3"`). Notably, Section 8/TR "Lisans ve Fikri Mülkiyet" (line 318+) already correctly described the Proprietary license — confirming this was isolated drift in the Executive Summary/Prior-Art sections, not a whole-document inconsistency. F3.2-02: line 14 (EN+TR) reworded to "source-available...released under a proprietary license (see Section 8)". F3.2-03: "React 18"→"React 19" in both; deliberately left "MIT License" on that row untouched, since the adjacent "Vite | MIT License" row confirms the table describes each *third-party tool's own* license (Vite genuinely is MIT), not SynCinema's — exactly the ambiguity the sub-plan's own risk section warned against over-correcting. F3.2-04: line 143 (EN+TR) reworded from "builds upon" to "draws on ideas from...(SynCinema's own source code remains proprietary — see Section 8)" to remove any implication SynCinema itself is open-source, without deleting the accurate prior-art claim (real GitHub links to Popcorn.js/Video.js/etc. as design influences, not code dependencies — confirmed via `package.json`, neither is an actual dependency). F3.2-05: final grep confirmed zero remaining "React 18" or SynCinema-self-open-source claims in either file. Docs-only change, no source-code gates affected. | none | Phase 3 fully closes once Faz3.3 completes (pending its live-test blocker). | — | 2026-07-22 |
| Planner-docs/Faz-3-Plans/Faz3.3-csp-dropbox-reconciliation.md | verified | Run 1 | Run 4 | Installed Playwright (user-approved, since CSP is browser-enforced and no code-trace substitute exists) and ran a real Chromium browser against the LIVE production site `syncinema.vercel.app`. F3.3-01/02: tested a synthetic (non-functional file ID, honestly disclosed) `dropbox.com` share-link-shaped URL — real CSP violation captured: `www.dropbox.com` itself is the blocked domain, NOT `dl.dropboxusercontent.com` as the Autopsy's static inference guessed (CSP blocks the *first* request, before any redirect could occur) — directly validating why a live test was mandatory. Also tested a real, genuinely public HTTPS video (Google's own commondatastorage.googleapis.com sample bucket, not fabricated) — also CSP-blocked, and a Google Drive regression case — correctly NOT CSP-blocked (0 violations; only an expected 403 from the fake file ID, proving CSP allows the request through and the failure is content-level, not policy-level). F3.3-03: widened `vercel.json`'s `media-src` with exactly `https://www.dropbox.com` (the domain the live test showed necessary, not a speculative addition). F3.3-04 (dual-fix, since Dropbox and generic-HTTPS are genuinely separate cases): also narrowed `UrlLoaderModal.tsx`'s generic-HTTPS messaging (hardcoded validation string + `supportedSources.https`/new `limitations.httpsCsp` i18n keys across all 4 languages: en/tr/az/ru) since arbitrary third-party HTTPS domains cannot be CSP-allowlisted without defeating CSP's purpose — the live test proved the old "Any HTTPS URL...will attempt to load" claim false. F3.3-05: could not redeploy to re-verify live (nothing pushed this session, consistent with prior Faz1.3/2.2/2.3 pattern) — instead built a local static server reproducing `vercel.json`'s exact headers (read from the file itself, not guessed) and re-ran the same Playwright suite against it: Dropbox now 0 violations (fix confirmed working), generic-HTTPS domain still correctly blocked (intended), Google Drive regression still unaffected. All test/server scratch files and screenshots cleaned up after. `typecheck`/`lint`/`build` all pass. | Live redeploy-and-reverify (true F3.3-05) still pending — requires the user to push and deploy before the fix is live in production. | Phase 3 fully closed (all 3 sub-plans verified). Push to `origin/main` needed before this fix is actually live. | — | 2026-07-22 |
| Planner-docs/Faz-4-Plans/Faz4.1-test-runner-bootstrap.md | verified | Run 1 | Run 5 (local) / Run 6 (live) | F4.1-01: installed `vitest@4.1.10`. F4.1-02: configured via `vite.config.ts`'s merged `test` field — had to import `defineConfig` from `vitest/config` and `loadEnv` from `vite` separately (vitest/config doesn't re-export loadEnv; first attempt importing both from vitest/config failed with a real startup error, caught and fixed). Set `environment: 'node'` explicitly (no jsdom, per Section 5's explicit deferral). F4.1-03: added `test`/`test:watch` scripts. F4.1-04: `src/utils/formatTime.test.ts` — one real assertion (`formatTime(90) === '1:30'`), `npm test` passes (1/1). F4.1-05: added `npm test` step to `.github/workflows/ci.yml` between lint and build, per the sub-plan's own fail-fast-ordering guidance. F4.1-06 (real CI run verification): **confirmed live 2026-07-22** — same GitHub Actions run referenced in Faz1.3's row (commit `5c0768f`, green check, 35s) included the `npm test` step passing for real, not just locally. `typecheck`/`lint`/`build` regression: all pass. | none | Faz4.2/4.3/4.4 may now begin — this sub-phase's `formatTime.test.ts` is explicitly the seed of Faz4.2's coverage, not standalone. | — | 2026-07-22 |
| Planner-docs/Faz-4-Plans/Faz4.2-fingerprint-time-tests.md | verified | Run 1 | Run 5 | F4.2-01: extended `formatTime.test.ts` to 7 cases (zero, sub-minute, minute-boundary, multi-minute w/ no hour-rollover, padding). F4.2-02/03: new `fileFingerprint.test.ts`, 15 cases covering `getVideoFingerprint`/`getAudioFingerprint` independently. **Real bug found and worked around correctly, not silently**: the `gdrive_` branch calls `new URL(objectUrl, window.location.origin)` — `window` doesn't exist in Vitest's Node environment, so the ReferenceError was being silently swallowed by the function's own try/catch, causing gdrive URLs to wrongly fall through to the `url_` branch. Reproduced this directly in plain Node first to confirm before writing any test. Fixed via a minimal test-file-local `globalThis.window` stub (one property only, not jsdom) rather than touching the source function's real (browser-only) runtime logic, which is out of Faz4.2's scope (test-writing, not bug-fixing) and works correctly in the actual browser. **Also confirmed a genuine, real divergence between the two functions** (not assumed): `getAudioFingerprint` has no `yt_` branch at all — a `youtube:`-scheme input falls through to `url_` — documented with an explicit test rather than silently matching `getVideoFingerprint`'s shape. F4.2-04: determinism + differentiation assertions included. Non-Latin1 URL case included per Section 11's specific risk callout. All 22 tests pass (7+15); `typecheck`/`lint`/`build` unaffected. | none | Faz4.3 may begin. | — | 2026-07-22 |
| Planner-docs/Faz-4-Plans/Faz4.3-srt-parsing-eq-preset-tests.md | verified | Run 1 | Run 5 | F4.3-01: extracted `parseSRT` verbatim (byte-identical logic) from `useVideoPlayer.ts`'s local closure into new `src/utils/srtParser.ts`, exported with a named `SrtCue` type; `useVideoPlayer.ts` now imports and calls it, no logic changes. F4.3-02 (manual regression, real browser this time — Playwright now available since Faz3.3): started `npm run dev`, uploaded a real 2-cue `.srt` file (one multi-line cue) through the actual `SubtitleSettings.tsx` file input in a live Chromium session — zero console/page errors, UI correctly flipped to the loaded state (`hasSubtitles=true`, confirmed via the "Replace" button appearing), screenshot captured. F4.3-03: `srtParser.test.ts`, 6 cases — well-formed multi-cue, single-cue, multi-line text, malformed timestamp (silent-skip asserted, explicitly flagged in a comment as a UX gap candidate per Section 8, not silently endorsed as ideal), too-few-lines (silent-skip asserted), empty input. F4.3-04: `eqPresets.test.ts`, 14 cases (`it.each` over all 6 presets for both `getCurrentPresetId` and `getPresetById`, plus one non-matching case and one unknown-id case). All 42 project tests pass (7+15+6+14); `typecheck`/`lint`/`build` unaffected. Scratch test script/files cleaned up after. | none | Faz4.4 may begin. | — | 2026-07-22 |
| Planner-docs/Faz-4-Plans/Faz4.4-drift-compressor-regression-tests.md | verified | Run 1 | Run 5 | F4.4-01: read the full "Handle Synchronization" effect in `AudioTrackRow.tsx` (not just grep) — confirmed the 500ms throttle gate and `driftWarning`/timeout state are side effects that stay in the component; only `targetTime`/`diff`/`shouldResync` math is pure and extractable, with all 4 inputs (`videoCurrentTime`, `track.offset`, `audioTime`, `syncThreshold`) explicitly closure-captured, none missed. F4.4-02: extracted `computeDriftCorrection` to `src/utils/driftCorrection.ts`; diff-reviewed the change — byte-identical expressions, only relocated. F4.4-03 (manual regression, real browser): live-tested via Playwright — added a real (minimal valid WAV) audio track, changed its offset to +2.0s, zero console errors across multiple real effect re-runs; full timed-resync-in-action wasn't exercised (headless timing control impractical), but combined with the exhaustive unit tests on the now-wired function this is a reasonably strong verification, documented honestly as partial. F4.4-04: `driftCorrection.test.ts`, 7 cases — **caught a real test-authoring bug during writing, not a source bug**: an initial boundary test used `10.3 - 10` expecting an exact `0.3`, but IEEE 754 float arithmetic makes that `0.30000000000000004`, failing the test; fixed by using power-of-two-friendly values (`10.5`/`0.5`) for a genuinely exact boundary, with the reasoning documented in the test file. Also covers negative offset, positive offset, and the floor-clamp (including resync-against-the-floored-value case). F4.4-05: also extracted the compressor-bypass rewire into `src/utils/compressorBypass.ts` (same pattern as the other 2 extractions this phase, since the sub-plan's own "no full-component testing" instruction requires an independently-testable unit) — `compressorBypass.test.ts`, 4 cases with mock `AudioNode`-like objects (`vi.fn()` for connect/disconnect) confirming the exact graph-rewire invariant (high→comp→gain when enabled, high→gain direct when disabled, both directions always disconnect first) per `Project-Ontology.md` Section 7. F4.4-06: untested `AudioGraphManager.tsx` surface documented explicitly in a code comment at the top of the test file (full graph init, EQ param application, gain-boost, device routing via setSinkId, AudioContext lifecycle) — not silently implied covered. All 53 project tests pass; `typecheck`/`lint`/`build` unaffected. | none | **Phase 4 complete** — Autopsy's entire ordered pure-function backlog (fingerprint→time→SRT→EQ→drift) plus the compressor-bypass invariant are all covered. Faz5.1 may begin. | — | 2026-07-22 |
| Planner-docs/Faz-5-Plans/Faz5.1-state-architecture-evidence-review.md | verified | Run 1 | Run 6 | F5.1-01: exact `SidebarProps` audit — 43 declared props (not the Autopsy's approximate "30+"), all 43 passed from `App.tsx`'s single `<Sidebar>` call site; only 39 are actually destructured/used inside `Sidebar.tsx` itself — `accentTheme`, `onAccentThemeToggle`, `isHelpOpen`, `onHelpClose` are declared and passed but never referenced in the file body (dead prop-passing, likely left over from when Help/theme controls moved into the separate `HelpPanel` component). Subsystem breakdown: video 10, audio 10, subtitles 6, markers 4, project/cloud-sync 3, UI/theme/help 9 (4 of these dead), analytics 1. F5.1-02: full 94-commit history scanned (`git log --oneline` + keyword grep for prop/thread/drill/sidebar, plus a file-count-vs-line-count heuristic pass for "many files touched for one small change" commits) — no incident found where a new prop had to be threaded through many files/components; the one related commit (`a74a662`, "split Sidebar component into domain-specific sub-components") was a component-decomposition-for-file-size refactor, not a state-architecture change, and did not reduce or restructure the prop-passing pattern. F5.1-03/04: decision and trigger condition recorded in Section 8 below. | none | Phase 5 complete (decision-only, no source code changed). Phase 6 may proceed. | — | 2026-07-22 |
| Planner-docs/Faz-6-Plans/Faz6.1-error-reporting-decision.md | verified_local_only | Run 1 | Run 6 | F6.1-01: presented 3 options (Sentry free tier / minimal self-hosted logging / explicit defer) to the user; **user chose Sentry free tier**. F6.1-02: installed `@sentry/react@10.67.0` (confirmed React 19-compatible peer range); new `src/lib/sentry.ts` reads `VITE_SENTRY_DSN` and no-ops safely when unset (same pattern as `src/lib/supabase.ts`'s env-var-absence tolerance); `initSentry()` called once in `main.tsx` before render; `ErrorBoundary.tsx`'s existing `componentDidCatch` now also calls `reportError(error, { componentStack })` alongside its existing `console.error`. Deliberately did **not** add manual `window.addEventListener('error'/'unhandledrejection', ...)` listeners as the sub-plan's Section 9 file table suggested — `Sentry.init()`'s default `GlobalHandlers` integration already covers both cases, so hand-rolled listeners would only duplicate-report the same errors; this is a documented deviation from the sub-plan's literal wording, not a missed requirement. F6.1-03 (privacy/data-sensitivity review): default `Sentry.init()` config used, no `browserTracingIntegration`/`replayIntegration`/performance monitoring added (out of scope per Section 5) and `sendDefaultPii` left at its default `false`, so no IP address or additional PII is sent; default capture is error stack trace + message, breadcrumbs (console output, clicks, navigation — the same non-sensitive debug info already visible in devtools, not raw `.sync` file contents or the Supabase anon key), URL, and user agent. No new class of sensitive data is exposed beyond what already prints to the browser console. F6.1-04 (live capture test): **not performed** — this environment has no Sentry account/DSN (same live-credential gap as Faz2.2/Faz2.3); `sentryEnabled`/`initSentry`/`reportError` verified locally instead via 3 new Vitest cases confirming safe no-op behavior when `VITE_SENTRY_DSN` is unset (the actual current state). `npm run typecheck`/`lint` (0 errors, 22 warnings, unchanged)/`build`/`test` (56/56, +3 new) all pass. | No live Sentry account/DSN in this environment (hard technical blocker, same category as Faz2.2/Faz2.3's Supabase access gap). | User needs to create a free Sentry project, set `VITE_SENTRY_DSN` in their local `.env`/Vercel project env vars (git-ignored, same convention as `VITE_SUPABASE_*`), then throw a deliberate test error in a non-production build to confirm it appears in the Sentry dashboard (completes F6.1-04). | — | 2026-07-22 |
| Planner-docs/Faz-6-Plans/Faz6.2-proxy-observability.md | verified | Run 1 | Run 6 | F6.2-01: this environment has no live Vercel dashboard/account access, so the audit is based on Vercel's documented platform behavior (function invocation logs, error/status counts, and duration are retained and reviewable per-deployment with no code change required) rather than a live login. F6.2-02: decided Vercel's built-in observability is sufficient — no concrete gap was found, consistent with the sub-plan's own default-toward-sufficient guidance for a low-traffic project; **no forwarding to Faz6.1's Sentry was implemented** (F6.2-03 not applicable). F6.2-04: added an explicit tradeoff-acceptance comment directly above the rate-limiter constants in `api/proxy.ts` (per-instance/cold-start-reset limitation accepted at current scale; revisit trigger = sustained traffic growth or a real abuse incident) plus this ledger record — both, since the sub-plan allowed either. F6.2-05: could not trigger a live rate-limit hit against the real deployed function (no live access); instead verified the mechanism itself locally — esbuild-transpiled `api/proxy.ts` into an ad hoc scratch script (cleaned up after, same disposable-script pattern as Faz2.1/Faz2.5) and called the exported handler 11 times with a mocked request sharing one simulated client IP: requests 1-10 passed the rate-limit check (400 missing-id, proving they reached past it), request 11 was correctly rejected with `429` + `Retry-After: 60`, and the exact `console.warn('[Proxy] Rate limited IP: ...')` line Vercel's dashboard would surface fired as expected. `npm run typecheck`/`lint` (0 errors, 22 warnings, unchanged)/`build` all pass after the comment-only `api/proxy.ts` change. **Live confirmation completed 2026-07-22:** user checked both Vercel pages directly. **Logs** tab showed real-time `/api/proxy` requests with `[Proxy] Success! Content-Type: video/mp4` messages (the exact `console.log` lines from the source) and `206` responses (range/seek requests working correctly). **Observability > Overview** showed Vercel Functions error rate 0%/timeout 0%, Edge Requests graph, and confirmed the Free-tier retention limit ("Upgrade to Pro... 30-day retention") — consistent with Faz6.3's independently-confirmed Free-tier finding. This fully validates F6.2-01/02's decision: Vercel's built-in dashboard genuinely is sufficient, no forwarding needed. One unrelated, non-blocking observation surfaced in Logs: a Node.js `DeprecationWarning: 'url.parse()' behavior is not standardized...` — not from this project's own code, informational only, no action needed. | none | Phase 6 fully live-verified (Faz6.1 code-verified/DSN pending, Faz6.2 fully verified, Faz6.3 verified). | — | 2026-07-22 |
| Planner-docs/Faz-6-Plans/Faz6.3-backup-retention-policy.md | verified | Run 1 | Run 6 | F6.3-01: this environment has no live Supabase dashboard/CLI access (same gap as Faz2.2/Faz2.3/Faz6.2); asked the user directly rather than assuming — **confirmed: Free tier**, which per Supabase's documented tier behavior includes no automatic backups/point-in-time recovery. F6.3-02: risk assessed as genuinely low — `sync_presets` is community-contributed, votable, re-derivable data (any user can re-submit the same offset), not user-private or irreplaceable, consistent with `Project-Ontology.md` Section 4. F6.3-03: policy decision written into `supabase/README.md`'s new "Backup/retention policy for `sync_presets`" section — **accept the zero-backup risk at the current Free tier; no application-level backup pipeline added**, with 3 explicit named revisit triggers (upgrade to Pro tier, `sync_presets` starts holding non-re-derivable data, or a real data-loss incident occurs — at which point a manual `pg_dump`/CSV export is the named first-reach mitigation, not full automation). Docs-only change; no source-code validation gates apply per this sub-plan's own Section 9 (no code changed). | none — this was a live-fact confirmation via direct question to the user, not a technical blocker. | **Phase 6 complete** (Faz6.1 verified_local_only, Faz6.2 verified_local_only, Faz6.3 verified). Phase 7 remains explicitly deferred (Faz7.1); do not expand without a new, explicit user signal. | — | 2026-07-22 |
| Planner-docs/Faz-7-Plans/Faz7.1-commercialization-gate-deferred.md | deferred | Run 1 | — | none | Explicitly deferred by user's own stated position during Step 1 intake; must not be expanded without a new, explicit user signal. Not a technical blocker — status wording corrected from "blocked" to "deferred" in Run 6 per Sub-Planing-Audit.md's AUDIT-FIX-01 (P3, resolved), aligning with Main-Planing.md/Sub-Planing-Index.md/Faz7.1's own consistent framing. | None — do not proceed without explicit user signal. | — | 2026-07-22 |

## 5. Implementation Runs

| Run | Date | Scope | Slices Completed | Notes |
|---|---|---|---|---|
| Run 2 | 2026-07-22 | Step 4, Phase 1 (user-approved batch: all 4 Phase 1 slices, checkpoint after each). | Faz1.1 (verified), Faz1.2 (verified), Faz1.3 (verified_local_only), Faz1.4 (verified) — **Phase 1 complete.** | AUDIT-FIX-01/02/03 remain open and visible per Sub-Planing-Audit.md; none touched by these slices. Committed locally as `633c9b4` at user's request; not pushed to `origin/main`. |
| Run 3 | 2026-07-22 | Step 4, Phase 2 (continuing after user-approved pause/save checkpoint; one major phase batch). | Faz2.1 (verified), Faz2.2 (verified_local_only), Faz2.3 (verified_local_only), Faz2.4 (verified), Faz2.5 (verified) — **Phase 2 complete.** | AUDIT-FIX-02 resolved (see Section 8). Faz2.2/Faz2.3's live-Supabase steps are blocked in this environment (no credentials) and recorded as follow-ups, not silently dropped. AUDIT-FIX-01/03 remain open/visible, untouched. |
| Run 4 | 2026-07-22 | Step 4, Phase 3 (documentation/CSP reconciliation). | Faz3.1 (verified), Faz3.2 (verified), Faz3.3 (verified) — **Phase 3 complete.** | Playwright + Chromium installed (user-approved) for Faz3.3's live CSP verification against production. Committed locally as `5d496a1`; not pushed to `origin/main`. Backfilled into this table at Run 6 for ledger completeness (originally recorded only in Section 4's status matrix and Section 6's snapshot). |
| Run 5 | 2026-07-22 | Step 4, Phase 4 (test coverage expansion for core sync logic). | Faz4.1 (verified_local_only), Faz4.2 (verified), Faz4.3 (verified), Faz4.4 (verified) — **Phase 4 complete.** | Vitest introduced; 53 project tests passing. Committed locally as `d0e642e`; not pushed to `origin/main`. Backfilled into this table at Run 6 for ledger completeness (originally recorded only in Section 4's status matrix and Section 6's snapshot). |
| Run 6 | 2026-07-22 | Step 4, Phase 5 + Phase 6 (fresh session resuming after Phases 1-4 from prior sessions; default batch = Faz5.1 + all 3 Phase 6 slices, checkpoint after each). | Faz5.1 (verified) — **Phase 5 complete.** Faz6.1 (verified_local_only) — Sentry wired, live DSN setup/test pending user. Faz6.2 (verified_local_only) — rate-limit mechanism verified locally, live Vercel dashboard confirmation pending user. Faz6.3 (verified) — **Phase 6 complete**, backup policy documented after user confirmed live Free-tier status. | **Entire READY/READY_WITH_WARNINGS queue now exhausted (20/20 active sub-plans verified across Runs 2-6).** Only Faz7.1 (DEFERRED, correctly excluded) remains. AUDIT-FIX-01 applied this run (see Section 4/8); AUDIT-FIX-03 remains open/visible (advisory-only, no action required). |

## 6. Current State Snapshot

- Repository: SynCinema, `main` branch. Phase 1 (Faz1.1-1.4) committed locally as `633c9b4`. Phase 2 (Faz2.1, Faz2.2 local-only, Faz2.3 local-only, Faz2.4, Faz2.5) committed locally as `75d06fc`. Phase 3 (Faz3.1, Faz3.2, Faz3.3) committed locally as `5d496a1`. Phase 4 (Faz4.1-4.4) committed locally as `d0e642e` ("chore(phase4): add Vitest and regression coverage for core sync logic"), at the user's explicit request. None of the four commits have been pushed to `origin/main` — the Faz3.3 CSP fix specifically is not live in production until this happens, and Faz1.3/Faz4.1's CI workflow has never run for real. Only the untracked `Planner-docs/` set from Step 2 remains in the working tree.
- Build/validation gates: `typecheck` (`tsc --noEmit`) and `build` (typecheck-gated) landed via Faz1.1; `lint` (ESLint, scoped to `rules-of-hooks`+`exhaustive-deps` for react-hooks, plus TS/react-refresh correctness rules) landed via Faz1.2, exits 0 with 22 accepted warnings (unused-vars, exhaustive-deps in sync-critical files left unfixed to avoid unverified behavior change, react-refresh fast-refresh hints — count dropped from 27 after Faz1.4 removed dead code with its own unused-var warnings). `.github/workflows/ci.yml` authored via Faz1.3 but not yet pushed/verified live. `wavesurfer.js` dependency and the dead `UrlLoaderModal.tsx` audio-tab code path removed via Faz1.4. No `test` script yet — Faz4.1 will establish that.
- Browser/UI testing infrastructure: none exists in this environment (no chromium-cli, no Playwright, no local browser binary) — Faz1.4's manual golden-path check was substituted with a full diff review instead of a live browser run; Phase 4's future test-runner work (Faz4.1+) is unit/logic-level only and does not by itself close this gap. Flagged as an open follow-up below.
- Security posture: proxy referer bypass (Faz2.1) and admin RPC brute-force (Faz2.2) are both closed — Faz2.2's per-IP lockout is live on top of a pre-existing global lockout discovered during Run 6 (see Section 8).
- Documentation: README and whitepaper drift confirmed but not yet corrected, pending Faz3.1/Faz3.2.
- No live/production changes have been made as part of this planning run — Step 2 is planning-only, per ClaudeQB safety rules.
- State architecture: Phase 5's evidence review (Faz5.1, Run 6) is complete — decision is **keep the current hooks-only composition as-is**; no React Context or state library introduced. See Section 8 for the full decision record and future trigger condition.
- Observability: Faz6.1 (Run 6) wired `@sentry/react` into `ErrorBoundary.tsx` and app startup (`main.tsx`), gated behind an unset-by-default `VITE_SENTRY_DSN` env var. Code path is locally verified (typecheck/lint/build/test all pass, 3 new Vitest cases); the live DSN has not been provisioned in this environment, so production errors are not yet actually reaching a Sentry dashboard — see Section 8 for the follow-up needed to complete this.

## 7. Replanning Inputs

Future Step 2 replanning sessions (if the Main Plan itself needs revision) or Step 4 implementation sessions should read, in order:
1. This ledger (Section 4's Sub-Plan Status Matrix) for current sub-plan status before starting or resuming any slice.
2. `Planner-docs/Main-Planing.md` for the confirmed vision, target end state, and phase roadmap — unchanged by Step 2.
3. `Planner-docs/Autopsy.md` and `Planner-docs/Project-Ontology.md` for repository evidence and invariants referenced throughout the sub-plans.
4. `Planner-docs/Sub-Planing-Index.md` for the phase-to-sub-plan map and priority detailing order.
5. Only the specific `Faz<n>.<m>-*.md` sub-plan file relevant to the active slice — not the entire `Faz-*-Plans/` tree at once, per Step 4 token-discipline guidance in `references/workflow-quality.md`.

## 8. Open Decisions and Follow-Ups

- **AUDIT-FIX-02 resolved (migration numbering, decided at Phase 2 kickoff in Run 3, before either Faz2.2 or Faz2.3 was implemented):** this environment has no Supabase CLI and no `.env`/credentials of any kind (confirmed via repository check), so Faz2.3's F2.3-02 live export (which would produce the true `0000_baseline_asis.sql` pre-fix snapshot) cannot be performed in this session. Decision: Faz2.2's new lockout mechanism is numbered `supabase/migrations/0001_admin_lockout_serverside.sql`, and `0000_baseline_asis.sql` is explicitly reserved for Faz2.3's export whenever the user (who has live dashboard/CLI access) performs it — ideally before applying `0001` live, so the baseline captures pre-fix reality as the sub-plan intends. This is a recorded decision, not a silent numbering collision.
- **Autonomy/review cadence for Step 4** — re-confirmed at the start of Run 2: user chose "Phase 1, all 4 slices" as the batch, checkpointing after each slice.
- **Faz1.3 live CI verification (F1.3-02/F1.3-03) and branch-protection recommendation (F1.3-04)** — `.github/workflows/ci.yml` is authored and locally pre-checked (typecheck+lint+build all pass), but no real GitHub Actions run has occurred and no throwaway-branch negative-path proof was performed; user explicitly chose to leave this local-only rather than push now. Separately, once a real Actions run exists, the user should consider enabling "require status checks to pass" branch protection on `main` in GitHub's repository settings — this is a GitHub UI setting outside this repo's files and cannot be performed by ClaudeQB.
- **Vitest introduction timing (Phase 4)** — `Main-Planing.md` Section 9 flags this as needing human confirmation: introduce now per the roadmap's sequencing, or defer until Phase 1/2 fully land. Faz4.1 is written assuming Phase 1 (specifically Faz1.3's CI) is already in place.
- **Error-reporting vendor choice (Faz6.1) — decided in Run 6: Sentry (free tier).** User chose Sentry over minimal self-hosted logging or explicit deferral. Wiring is complete and locally verified; remaining follow-up is purely operational: user must create a Sentry project, set `VITE_SENTRY_DSN` (local `.env` + Vercel project env vars, git-ignored like `VITE_SUPABASE_*`), and confirm a deliberately-thrown test error appears in the Sentry dashboard (F6.1-04).
- **Faz2.2 live-execution: done (Run 6).** See the plan-vs-reality discovery entry above/below. **Faz2.3 live-execution: partial** — baseline captured for 2 of 4 RPCs only, still needs `safe_insert_preset`/`safe_increment_vote`/RLS policies.
- **Plan-vs-reality discovery (Run 6, major):** `verify_admin_password`/`admin_delete_preset` already had a live, working, GLOBAL brute-force gate (`login_attempts` table: 5 failures/5min across all callers, 1s throttle, hashed password) that no ClaudeQB planning document knew about — Step 2 had no live Supabase access when Autopsy's AUTOPSY-P1-01 assumed zero server-side protection existed. This did not invalidate the finding entirely (the *client-side-only, directly-bypassable-via-raw-RPC-call* framing was accurate), but it means the actual pre-fix risk was smaller than assumed. **Lesson for future replanning:** Autopsy-stage findings about live backend behavior (Supabase RPC bodies, in this case) carry lower confidence than findings about local repo files, since Autopsy itself has no live access — treat such findings as hypotheses to verify at Step 4, not settled fact, consistent with `Project-Comprehension.md`'s tentative-claim methodology (though that file was never created for this project). User chose to keep the existing global gate untouched and add the planned per-IP gate as an additional layer rather than picking one over the other.
- **Faz3.3 resolved** — see status matrix. CSP fix (`vercel.json` +`https://www.dropbox.com`) and UI messaging correction are complete and locally verified (via a local server reproducing `vercel.json`'s real headers), but not yet live: needs a push + Vercel deploy before production actually carries the fix.
- **Phase 5 (state-architecture) expansion** — intentionally not expanded beyond Faz5.1's evidence-review sub-plan; any decision to introduce scoped Context is Faz5.1's own output, not predetermined here.
- **Phase 7 (commercialization) expansion** — explicitly deferred (not a technical blocker); must not be expanded without a new, direct user signal that commercialization intent is becoming concrete, per `Main-Planing.md` Section 6/Section 9 and Faz7.1's own content. (AUDIT-FIX-01 resolved in Run 6: this Section's and Section 4's wording now consistently say "deferred," matching every other source document.)
- **Browser/UI testing infrastructure now exists** — Playwright + Chromium were installed during Faz3.3 (user-approved) since CSP verification has no code-trace substitute. This resolves the gap noted after Faz1.4 for any future sub-plan needing a real browser check; Faz4.3 already benefited from it (real browser SRT-upload regression check, not a code-trace this time). Faz4.1 decided Vitest as the unit-test runner; Playwright is currently ad hoc/manual-verification tooling only, not wired into any test script or CI step — a future decision if E2E automation is ever wanted.
- **`parseSRT` malformed-timestamp/too-short-block silent-skip is a real UX gap candidate (from Faz4.3, not fixed — out of that sub-phase's scope)** — a `.srt` file with one malformed cue (bad timestamp format, or a block missing its text line) silently loses just that cue with zero user-facing warning; the rest of the file parses fine. Confirmed as current, regression-tested behavior via `srtParser.test.ts`, not silently endorsed as correct. Worth a future decision on whether to surface a "N cues could not be parsed" warning to the user.
- **Faz5.1 decision (Phase 5, decided in Run 6): keep hooks-only state composition as-is.** Do not introduce React Context or any state-management library at this time. Rationale: the exact current `Sidebar.tsx` prop count is 43 (not just the Autopsy's approximate "30+"), of which 4 (`accentTheme`, `onAccentThemeToggle`, `isHelpOpen`, `onHelpClose`) are declared and passed by `App.tsx` but never actually consumed inside `Sidebar.tsx` — flagged as a minor dead-prop cleanup candidate for a future Faz1.4-style slice, not fixed here (out of this sub-phase's scope). A full 94-commit history scan found zero incidents of a new feature requiring a prop threaded through many files/components; the only related commit (`a74a662`, Sidebar component split) was a file-size/maintainability refactor, not a state-architecture response, and left the flat prop-passing pattern unchanged. Raw prop count alone was explicitly rejected as a sufficient trigger, since count without an incident was not evidence of pain in this review. **Future trigger condition (revisit when true):** a concrete new feature requires threading a new prop through 4+ intermediate components to reach its consumer, or a second such friction incident is observed — whichever comes first. Any future scoped-Context work must be re-confirmed with the user before a new Faz5.2 sub-plan is authored (per Faz5.1 Section 10/13); Phase 5 is not re-opened by this decision.
- **Run 2's Phase 1 changes are committed but unpushed** — all of Faz1.1–1.4's code changes were committed locally as `633c9b4` ("chore(phase1): enforce typecheck/lint/CI gates, remove dead weight") on `main`, at the user's explicit request to pause and save before continuing to Phase 2. Not pushed to `origin/main` (matches the user's earlier choice on Faz1.3 to keep CI verification local-only for now). `Planner-docs/` was deliberately left untracked/uncommitted — not bundled into this commit, left as a separate decision for the user.
